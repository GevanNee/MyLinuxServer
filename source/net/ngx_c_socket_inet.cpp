
#include "ngx_c_socket.h"
#include "ngx_func.h"
#include "ngx_macro.h"

size_t CSocket::ngx_sock_ntop(struct sockaddr* sa, int port, u_char* text, size_t len)
{
	ngx_log_core(NGX_LOG_EMERGENCY, 0, "CSocket::ngx_sock_ntop函数还没写完！！！！");
	return 0; 
}